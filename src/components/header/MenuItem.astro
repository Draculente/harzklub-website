---
export interface Props {
  href: string;
  name: string;
  currentPage: boolean;
}

const { href, name, currentPage } = Astro.props;
---

<a
  href={href}
  class={currentPage
    ? "underline md:menu-item text-gray-700"
    : "md:menu-item text-gray-700"}>{name}</a
>
