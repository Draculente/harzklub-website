---
export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<p class="chip chip-green sm:p-1.5">{title}</p>

<style>
  /* p {
    color: var(--color-text-i);
    font-size: var(--font-size-xs);
    border-radius: 30px;
    padding-inline: 8px;
    padding-block: 3px;
    width: fit-content;
    background-color: var(--color-accent);
  } */
</style>
